/** @odoo-module **/
//
// This file's only job: import the component module so Odoo's bundler
// includes it and executes the registry.category("main_components").add(...)
// call at startup.
//
// All logic lives in smart_calculator_component.js.

import "./smart_calculator_component";